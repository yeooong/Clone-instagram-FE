import React from 'react';
import { SModalHeader } from 'src/styles/PostModal.styled';

function ModalHeader({ children }) {
  return <SModalHeader>{children}</SModalHeader>;
}

export default ModalHeader;
